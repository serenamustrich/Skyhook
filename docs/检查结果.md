检查完了，结论先说狠一点：MiMo 这轮不能算通过，需要返工。 cargo check --tests 能过，但 cargo test --all-targets 已经失败了，而且 NativeL3 有几个会影响真实可用性的硬伤。
主要问题
NativeL3 拿错 utun 接口名 [native_tun.rs (line 105)](/Users/chency/Downloads/clash/Skyhook/src/inbound/native_tun.rs:105) 创建 utun 后，实际接口名只在 [native_tun.rs (line 519)](/Users/chency/Downloads/clash/Skyhook/src/inbound/native_tun.rs:519) 里读出来并打日志，但没有返回给 setup。后面用 get_interface_name()，没指定名字时会变成 "utun"，不是实际的 utun7/utun8。这会导致 ifconfig/route 配置打到不存在的接口上。
bypass 路由逻辑是反的 [native_tun_system.rs (line 123)](/Users/chency/Downloads/clash/Skyhook/src/inbound/native_tun_system.rs:123) 把 bypass 和 route_exclude 生成为 RouteExclude，但 [native_tun_system.rs (line 181)](/Users/chency/Downloads/clash/Skyhook/src/inbound/native_tun_system.rs:181) 的命令还是 route add ... -interface <tun>。这不是绕过 TUN，而是把内网/排除网段也塞进 TUN，可能直接搞坏局域网访问，甚至让代理服务器流量回环。
/skyhook/tun/status 和 metrics 是假数据 [core/mod.rs (line 176)](/Users/chency/Downloads/clash/Skyhook/src/core/mod.rs:176) 每次临时构造 metrics，所有包数/字节/drop 都写死 0；[api/mod.rs (line 470)](/Users/chency/Downloads/clash/Skyhook/src/api/mod.rs:470) 只是把这些假 metrics 返回。NativeTunMetrics 结构体写了，但没接到 native_tun::serve() 的 read/write loop，也没挂进 Runtime。
packet-to-flow 智能分流还没真正工作 [native_tun.rs (line 293)](/Users/chency/Downloads/clash/Skyhook/src/inbound/native_tun.rs:293) 只按 IP 协议粗略建 flow，端口全是 0，没有真正解析 TCP/UDP 源/目标端口，也没用 TLS SNI/HTTP Host。更关键的是 [native_tun.rs (line 323)](/Users/chency/Downloads/clash/Skyhook/src/inbound/native_tun.rs:323) 虽然算了 decision，但后面仍然无条件 send_l3_ip_packet() 给同一个 L3 profile，所以“按 app/domain/ip 走指定节点”在 NativeL3 里还没有生效。
完整测试失败 cargo test --all-targets 失败 2 个：
select_group_snapshot_reports_best_latency
dns_exchange_uses_tcp_framing_through_runtime
代理组失败点在 [core/mod.rs (line 1563)](/Users/chency/Downloads/clash/Skyhook/src/core/mod.rs:1563)，select 组现在被当手动组返回第一个节点，但测试期望按延迟选 fast。DNS 失败点和 [core/mod.rs (line 1065)](/Users/chency/Downloads/clash/Skyhook/src/core/mod.rs:1065) 有关：dns_upstream() 优先取默认 nameserver/default_nameserver，导致测试里设置的 dns.server 被忽略，跑去连默认 8.8.8.8:53。
Hysteria v1 状态不一致 代码里新增了 HysteriaOutbound，见 [outbound/mod.rs (line 1779)](/Users/chency/Downloads/clash/Skyhook/src/outbound/mod.rs:1779)，但 README 仍写 Hysteria v1 planned/not implemented。更重要的是现在没有真实协议级集成测试，不能轻易说“真实拨号完成”。
验证结果
cargo check --tests: 通过，但有 4 个 warning。
cargo run -- check -c skyhook.example.yaml: 通过。
cargo test --all-targets: 失败，2 个 integration test 挂了。
建议让 MiMo 先返工顺序
先修两个失败测试，保证基础行为不退化。
修 NativeL3 实际接口名返回，create_tun_interface() 应该直接返回 NativeTunDevice。
修 bypass/route_exclude，不要把 bypass 网段 route 到 TUN。
把 NativeTunMetrics 接到 Runtime 和 native_tun::serve()，让 API 返回真实包数/字节/drop。
  5.	暂时不要宣称 NativeL3 智能分流完成，先把 TCP/UDP port、DNS mapping、SNI/Host 和 decision 生效链路补上。
